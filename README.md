# Послание от Хранителей — Telegram bot

Environment variables:
- BOT_TOKEN — токен от BotFather
- PUBLIC_URL — адрес Render-сервиса, например https://hranitel-bot.onrender.com

Build: `pip install -r requirements.txt`
Start: `python app.py`

После первого deploy открой `/set-webhook` один раз.
